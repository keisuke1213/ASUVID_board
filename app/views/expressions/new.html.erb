<%= form_with model: @exp do |f| %>

　　<h6>画像を選択</h6>
    <%= f.file_field :exp_image, placeholder: "画像", accept: "image/*" %>
    
    <h4>活動名</h4>
    <%= f.text_field :title %>

    <h4>体験記</h4>
    <%= f.text_area :content%>
    
    
    <%= f.submit '登録' %>
  <% end %>
